# Frozen API contract tests — safety net for refactoring.
# These tests validate response shapes, Redis channel patterns,
# webhook formats, and internal callback payloads as documented
# contracts that must not change during the refactoring.
